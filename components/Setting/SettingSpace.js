import React from "react";
import { View, StyleSheet } from "react-native";

const SettingSpace = (props) => {
  return <View style={styles.container} />;
};

const styles = StyleSheet.create({
  container: {
    height: 20,
  },
});

export default SettingSpace;
